import { UserNameApiDirective } from './user-name-api.directive';

describe('UserNameApiDirective', () => {
  it('should create an instance', () => {
    const directive = new UserNameApiDirective();
    expect(directive).toBeTruthy();
  });
});
